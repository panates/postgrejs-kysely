diff --git a/test/node/src/test-setup.ts b/test/node/src/test-setup.ts
index 85328bf..e1ecfd8 100644
--- a/test/node/src/test-setup.ts
+++ b/test/node/src/test-setup.ts
@@ -8,6 +8,8 @@ import * as Tarn from 'tarn'
 import * as Tedious from 'tedious'
 import type { PoolOptions } from 'mysql2'
 import { PGlite } from '@electric-sql/pglite'
+import { PostgrejsDialect } from 'kysely-postgrejs'
+import { DataTypeOIDs, Pool as PostgrejsPool } from 'postgrejs'
 
 chai.use(chaiAsPromised)
 
@@ -196,13 +198,21 @@ export const PG_ERRORS: Error[] = []
 
 export const DB_CONFIGS: PerDialectVariant<KyselyConfig> = {
   postgres: {
-    dialect: new PostgresDialect({
-      controlClient: Client,
+    // PATCHED: the postgres variant runs on kysely-postgrejs instead of
+    // the built-in pg dialect, so the whole suite exercises that driver.
+    dialect: new PostgrejsDialect({
+      // The suite is written against pg, which hands back int8 as a string;
+      // this asks PostgreJS for the same thing.
+      fetchAsString: [DataTypeOIDs.int8],
       pool: async () =>
-        new Pool(DIALECT_CONFIGS.postgres).on('error', (error) =>
-          PG_ERRORS.push(error),
-        ),
-      cursor: Cursor,
+        new PostgrejsPool({
+          host: DIALECT_CONFIGS.postgres.host,
+          port: DIALECT_CONFIGS.postgres.port,
+          user: DIALECT_CONFIGS.postgres.user,
+          password: DIALECT_CONFIGS.postgres.password,
+          database: DIALECT_CONFIGS.postgres.database,
+          max: POOL_SIZE,
+        }).on('error', (error: Error) => PG_ERRORS.push(error)),
     }),
     plugins: PLUGINS,
   },
diff --git a/test/node/src/transaction.test.ts b/test/node/src/transaction.test.ts
index 1b19740..bc03242 100644
--- a/test/node/src/transaction.test.ts
+++ b/test/node/src/transaction.test.ts
@@ -22,6 +22,10 @@ import {
   DIALECTS,
 } from './test-setup.js'
 import { DatabaseError as PostgresError } from 'pg'
+// PATCHED: the postgres variant runs on kysely-postgrejs, so the driver the
+// stub has to reach and the error class the driver throws are both its own.
+import { DatabaseError as PostgrejsError } from 'postgrejs'
+import { PostgrejsDriver } from 'kysely-postgrejs'
 import { SqliteError } from 'better-sqlite3'
 
 for (const dialect of DIALECTS) {
@@ -260,7 +264,7 @@ for (const dialect of DIALECTS) {
         if (sqlSpec === 'sqlite') {
           expect(error).to.be.instanceOf(SqliteError)
         } else if (sqlSpec === 'postgres' && variant !== 'pglite') {
-          expect(error).to.be.instanceOf(PostgresError)
+          expect(error).to.be.instanceOf(PostgrejsError)
         }
 
         expect((error as Error).stack).to.include('transaction.test.js')
@@ -269,7 +273,7 @@ for (const dialect of DIALECTS) {
 
     it('should not attempt to rollback if transaction failed to begin', async () => {
       const driverProto = {
-        postgres: PostgresDriver,
+        postgres: PostgrejsDriver,
         mysql: MysqlDriver,
         mssql: MssqlDriver,
         sqlite: SqliteDriver,
